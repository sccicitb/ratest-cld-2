mcp_servers:
  - name: satudata-garut
    transport: streamable-http
    url: http://localhost:8800/mcp
    auth: { type: none }
    enabled: true      # flipped on in Stage 8 against the real server
    allowed_tools: []
