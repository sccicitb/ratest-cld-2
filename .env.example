# Base URL for the (currently mocked) backend API.
# All requests are intercepted in src/lib/mock.ts, so this value is unused
# until a real backend is wired into src/lib/api.ts.
VITE_API_BASE_URL=http://localhost:8000/api
